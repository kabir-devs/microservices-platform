replicaCount: 2

image:
  repository: ghcr.io/kabir-devs/microservices-platform/backend-java
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  port: 8080

ingress:
  enabled: true
  host: platform.local
  path: /api/catalog
  tlsSecretName: platform-tls

resources:
  requests:
    cpu: 250m
    memory: 384Mi
  limits:
    cpu: 1000m
    memory: 768Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 70

env:
  - name: SPRING_DATASOURCE_URL
    valueFrom:
      secretKeyRef:
        name: postgres-credentials
        key: jdbc-url
  - name: SPRING_DATASOURCE_USERNAME
    valueFrom:
      secretKeyRef:
        name: postgres-credentials
        key: username
  - name: SPRING_DATASOURCE_PASSWORD
    valueFrom:
      secretKeyRef:
        name: postgres-credentials
        key: password
  - name: SPRING_REDIS_HOST
    value: "redis.platform.svc.cluster.local"

podSecurityContext:
  runAsNonRoot: true
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: false
  capabilities:
    drop: ["ALL"]
